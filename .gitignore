# Buildroot (cloned and built by Makefile)
buildroot/

# Cross-compiled userspace binaries (src/Makefile output)
build/

# Datasheets (third-party, not ours to distribute)
resources/datasheets/*

# Multi-GB raw logic-analyzer captures — too big for git
resources/logic-analyzer/

# Serial-console capture files from minicom/tio
*.log
*.log_old
*.cap

# Kernel module build artifacts (src/drivers/*/)
*.ko
*.o
*.mod
*.mod.c
*.mod.o
modules.order
Module.symvers
.*.cmd
*.cmd
onboard
